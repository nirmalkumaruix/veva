import { Link, Outlet } from 'react-router-dom';import { Bell, Building2, Home, LogOut, Moon } from 'lucide-react';import { useAuth } from '../store/auth';import { Button } from './ui';
export function Layout(){const auth=useAuth();return <div className="min-h-screen kolam-bg"><nav className="sticky top-0 z-10 border-b bg-white/80 backdrop-blur dark:bg-slate-950/80"><div className="mx-auto flex max-w-7xl items-center justify-between px-4 py-3"><Link to="/" className="flex items-center gap-2 text-xl font-black"><Building2 className="text-kolam"/> Veetu Vadagai <span className="text-sm text-marigold">வீட்டு வாடகை</span></Link><div className="flex items-center gap-3"><Link to="/owner" className="hidden md:inline">Owner</Link><Link to="/tenant" className="hidden md:inline">Tenant</Link><Link to="/notifications"><Bell size={20}/></Link><button onClick={()=>document.documentElement.classList.toggle('dark')}><Moon size={20}/></button>{auth.token?<Button onClick={auth.logout} className="px-3"><LogOut size={16}/></Button>:<Link to="/login" className="font-semibold">Login</Link>}</div></div></nav><main className="mx-auto max-w-7xl px-4 py-8"><Outlet/></main></div>}
