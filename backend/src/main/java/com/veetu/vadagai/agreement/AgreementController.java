package com.veetu.vadagai.agreement;import com.veetu.vadagai.common.ApiResponse;import org.springframework.web.bind.annotation.*;@RestController @RequestMapping("/api/v1/agreements") public class AgreementController{@GetMapping public ApiResponse<String> list(){return ApiResponse.ok("Agreements module ready","Upload and lifecycle tracking endpoints are available for extension");}}
