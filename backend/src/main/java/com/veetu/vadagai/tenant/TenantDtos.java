package com.veetu.vadagai.tenant;import jakarta.validation.constraints.*;import java.time.LocalDate;import java.util.UUID;public class TenantDtos{public record TenantRequest(@Email @NotBlank String email,@NotBlank String fullName,String mobile,@NotNull UUID propertyId,String emergencyContact,LocalDate moveInDate){} public record TenantResponse(UUID id,String email,String fullName,String propertyTitle,String emergencyContact,LocalDate moveInDate){} }
