package com.veetu.vadagai.payment;import jakarta.validation.constraints.*;import java.math.BigDecimal;import java.time.LocalDate;import java.util.UUID;public class PaymentDtos{public record PaymentRequest(@NotNull UUID propertyId,@NotNull PaymentType type,@NotNull @Positive BigDecimal amount,@NotNull LocalDate dueDate){} public record PaymentResponse(UUID id,PaymentType type,PaymentStatus status,BigDecimal amount,LocalDate dueDate,String gatewayOrderId,String receiptUrl){} public record RazorpayWebhook(String orderId,String paymentId,String status,String signature){} }
